import XCTest
@testable import ShareSheetView

final class ShareSheetViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ShareSheetView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
